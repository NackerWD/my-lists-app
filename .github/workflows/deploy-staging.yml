name: Deploy staging

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Deploy backend a Railway
        run: echo "TODO configurar quan l'entorn estigui llest"

      - name: Deploy frontend a Vercel
        run: echo "TODO configurar quan l'entorn estigui llest"
