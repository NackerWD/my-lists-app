name: Deploy Staging
on:
  push:
    branches: [never-trigger-disabled, main-disabled]
jobs:
  placeholder:
    name: disabled
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploy gestionat per Railway i Vercel automàticament"
