/** @type {import('jest').Config} */
const config = {
  // el mateix contingut que jest.config.ts però sense types
  // canvia: export default { ... }
  // per:    module.exports = { ... }
}

module.exports = config
